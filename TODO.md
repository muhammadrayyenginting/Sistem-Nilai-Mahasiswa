# TODO - Hubungkan ke Google Sheets

- [ ] Tambahkan panel UI koneksi di `index.html` (input URL + tombol simpan/test + status connection)
- [ ] Isi default URL koneksi dengan link Apps Script yang diberikan
- [ ] Pastikan tombol memanggil `saveApiUrl()` dan sukses memuat data via `loadData()`
- [ ] Verifikasi elemen yang dipakai sudah ada: `api-url-input`, `connection-status`

